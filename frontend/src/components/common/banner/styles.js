import styled from 'styled-components';

// styled
import { OrangeButton } from '../../eventDetail/EventDetailSidebar/styles';

export const MeetupButton = styled(OrangeButton)`
  box-shadow: none;
  text-decoration: none;
  cursor: pointer;

  display: inline-flex;
  padding: 5px 15px;

  margin: 80px 0 0 0;
`;

export const Button = styled.div``;
